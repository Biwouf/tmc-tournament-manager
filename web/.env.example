# Variables d'environnement — site vitrine (`web/`)
#
# Copier ce fichier en .env.local puis renseigner les valeurs du projet Supabase de
# DÉVELOPPEMENT. Ne jamais committer de valeurs réelles : .env et .env.local sont gitignorés.

# URL du projet Supabase (dashboard → Project Settings → API)
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Clé publique « anon » du projet Supabase (dashboard → Project Settings → API)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Environnement applicatif : development | production
# En local, toujours garder development (pointe sur le projet Supabase de dev).
VITE_ENV=development

# Multi-tenant — slug du club à rendre en dev / preview. En prod le club est déduit du
# sous-domaine `<slug>.feelike.app` et cette variable est ignorée.
VITE_DEV_CLUB_SLUG=cac-tennis
