# CODEBASE.md — Carte d'architecture TMC Tournament Manager

> Fichier destiné à l'IA. Lire en premier avant toute intervention sur le code.
> **À maintenir à jour** à chaque changement de rôle d'un fichier ou création d'un nouveau fichier `src/`.

---

## Vue d'ensemble

Application React/TypeScript pour gérer des tournois de tennis TMC (Tournoi Multi-Chances). Six modules indépendants :

1. **TMC Planner** — configuration + génération automatique du planning multi-tournois
2. **Programmation Image** — import d'une feuille FFT/TEN'UP (PDF ou CSV) → export affiche JPEG
3. **Events** — CRUD backoffice d'événements du club (Supabase + Storage)
4. **Live Score** — saisie en temps réel du score d'un match (Supabase + Realtime pour PWA future)
5. **Actus** — CRUD backoffice des actualités du club (Markdown + multi-images, brouillon/publié)
6. **Matches par équipe** — gestion des rencontres interclubs (référentiel saisons/compétitions/équipes, rencontres, score, photos, bascule Live Score)

Stack : React 19, TypeScript, Vite, Tailwind CSS, Supabase (auth + DB + Storage), localStorage (persistance TMC), react-markdown, zod (validation de `club_settings.config`, PR6a).

---

## Structure `src/`

### Logique métier (pas de dépendances React)

| Fichier | Rôle |
|---|---|
| `types.ts` | Tous les types TypeScript du projet (`GlobalConfig`, `TournamentConfig`, `Match` — inclut `bracket: MatchBracket`, `MatchBracket`, `ScheduledMatch`, `Schedule`, `TournamentEntry`, `DailyTimeSlot`, `TennisRanking`, `Gender`, `ClubEvent`, `EventType`, `TeamMatch`, `TeamMatchGender`, `TeamMatchType` (ces 3 derniers servent à l'affiche du module Matches par équipe), `LiveMatch` — inclut `started_at: string \| null` (horodatage du démarrage, sert au tri "En live") et `retired_player: LiveMatchWinner \| null` (renseigné en cas d'abandon), `LiveMatchStatus`, `LiveMatchType`, `LiveSet3Format`, `LiveMatchWinner`, `Profile` (id/prenom/nom — gestionnaires de lives), `Actu` — inclut `image_captions` (légendes affichées en PWA), `ActuFocalPoint`, et les types du module Matches par équipe : `TeamSaison`, `TeamCompetition` (+ `TeamCompetitionNom`, `TeamType`, `TeamGenre`, `TeamCategorie`, `TeamFormat`), `TeamEquipe` (+ `TeamDivision`, `TeamStadeFinale`), `TeamEtape`, `TeamRencontre`, `TeamMatchLine` (+ `TeamJoueur`, `TeamMatchLineType`, `TeamMatchGagnant`)) |
| `tmcLogic.ts` | Génère les matchs TMC pour 4, 8, 12, 16 ou 24 joueurs. Entrée : `TournamentConfig`. Sortie : `Match[]`. Pas d'effet de bord. |
| `scheduler.ts` | Algorithme de planification : génère les créneaux horaires (`generateTimeSlots`), distribue les matchs (`generateSchedule`) et tente de placer les matchs non planifiés après ajustement manuel (`retryUnscheduledMatches`). La contrainte 4h est trackée par `(tournoi, bracket)`. La stratégie de remplissage est contrôlée par `GlobalConfig.slotFillingStrategy` (`'smooth'` ou `'max'`). |
| `moveMatch.ts` | Déplacement manuel d'un ou plusieurs matchs avec cascade automatique des tours suivants si la contrainte 4h est violée. Le check feeder utilise le bracket du match (helper `findFeederMatches`). Importe `generateTimeSlots` depuis `scheduler.ts`. |
| `exportScheduleCsv.ts` | Export d'un `Schedule` au format CSV (séparateur `;`, UTF-8 BOM pour Excel). Tri par date / heure / terrain ; déclenche le téléchargement du fichier. Consommé par `TournamentPage`. |
| `liveScoreRules.ts` | Règles pures de score tennis — état d'un set normal (ongoing/tiebreak/won), super tiebreak, incrément/décrément +/- et détection du vainqueur de match. Pas d'effet de bord. Consommé par `LiveScoreEntry` et `LiveMatchPage`. **Existe en double dans `pwa/src/liveScoreRules.ts`** — à maintenir synchronisé. |

### Pages (React Router)

| Fichier | Route | Rôle |
|---|---|---|
| `pages/LoginPage.tsx` | `/login` | Auth Supabase |
| `pages/AppHomePage.tsx` | `/` | Dashboard — cartes d'accès aux modules (Actus, Événements, Live Score, Matches par équipe, Affiche, Planning, Admin). **PR4** : chaque item de `SECTIONS` porte un champ `roles` (rôles autorisés à le voir) ; les items sont filtrés par le rôle effectif de `useClubRole()` (super-admin → `admin`), puis les sections vides sont retirées pour ne pas laisser de titre orphelin. Un `member` ne voit que la carte Live Score. **PR5** : la carte **« Console plateforme »** (section *Plateforme* → `/super-admin`) est rendue **hors de `SECTIONS`**, conditionnée à `isSuperAdmin` — c'est une surface plateforme, pas un module club : on ne l'ajoute pas à la matrice de rôles. **PR5-bis** : l'item `admin` « Inviter un utilisateur » (`/admin/invite`) devient **« Membres »** (`/admin/members`, `roles: ['admin']`) — l'invitation n'est plus une page mais un panneau de l'écran de gestion des membres. **Refonte Matches par équipe** : ajout de l'item **« Saisons & équipes »** (`/team-matches/admin`, `roles: ['admin', 'manager']`) dans la section *Actus du club*, sous « Matches par équipe ». C'est le **seul point d'entrée UI** du référentiel interclubs depuis que le bouton « Admin » a quitté le header de `TeamMatchesPage` : le brief le supposait déjà présent ici, il ne l'était pas, et la page n'était plus atteignable qu'en tapant l'URL. Placé dans *Actus du club* et non dans la section *Admin* — dont le libellé « Réservé aux administrateurs » serait faux — pour conserver l'accès aux `manager`, qui l'avaient via le header. |
| `pages/HomePage.tsx` | `/tmc-planning` | Liste des configurations sauvegardées |
| `pages/TournamentPage.tsx` | `/tmc-planning/:id` | Écran principal TMC Planner (config + schedule) |
| `pages/ProgrammationImagePage.tsx` | `/programmation-image` | Import PDF/CSV → rendu affiche → export JPEG. Le `Match` local porte un flag `wo: boolean` (walkover) ; les matchs WO sont conservés dans l'état source mais exclus de `displayMatches` (rendu/pagination) et de `transferableMatches` (compteur + payload). Détection PDF : token `"WO"` dans la slice X de la colonne-match ; détection CSV : colonne `wo` optionnelle (valeurs `WO`/`wo`/`1`/`true`/`oui`). Bouton « Basculer vers Live Score » : insère tous les matchs détectés dans `live_matches` (status `pending`, match_type `simple`) avec un événement optionnel. |
| `pages/EventsPage.tsx` | `/events` | Liste paginée des événements (toggle à venir / passés), actions modifier/dupliquer/supprimer |
| `components/EventForm.tsx` | `/events/new`, `/events/:id/edit` | Formulaire création/édition d'événement (markdown preview, upload image Supabase Storage) |
| `pages/LiveScorePage.tsx` | `/live-score` | Vue projetée sur TV en club (V1 TV Board). Header `← Accueil / Live Score` avec badge "N en cours" (rouge pulsant) si lives. 3 sections (En live / En attente / Terminés) en grid 2 colonnes ; header de section avec compteur coloré. Abonnement Supabase Realtime sur `live_matches`. Démarrer ouvre un dialog pour saisir le court. Batch-fetch des profils des gestionnaires actuels (`scored_by`) pour afficher leur nom dans le dialog *« Prendre le contrôle »* (warning quand l'opérateur clique sur le live d'un autre user). Container `max-w-[1400px]`. |
| `pages/LiveMatchPage.tsx` | `/live-score/:id` | Saisie du score d'un match avec `LiveScoreEntry`. Détection auto de fin de match. Bouton "Annuler la fin de match" si finished. Abonnement Realtime filtré sur l'ID du match : si `scored_by` change vers un autre user, bandeau d'alerte *« Vous êtes en lecture seule »* + `forceDisabled` passé au `LiveScoreEntry`. |
| `components/LiveMatchForm.tsx` | `/live-score/new` | Formulaire création d'un match (simple/double, joueurs, event lié optionnel parmi les events des 30 derniers jours). |
| `pages/ActusPage.tsx` | `/actus` | Liste des actus (brouillons + publiées) triées DESC, badges Brouillon/Publié, actions publier/dépublier/modifier/supprimer. |
| `components/ActuForm.tsx` | `/actus/new`, `/actus/:id/edit` | Formulaire création/édition d'actu (markdown preview, multi-images optionnelles avec sélection du point de focus par clic sur l'aperçu, deux boutons « Brouillon » / « Publier »). En création, lit `location.state = { titre?, image_urls? }` pour préremplir (utilisé par le bouton « Créer une actu » d'une rencontre par équipe). Au clic sur « Publier », une option « Publier aussi sur Facebook » (+ sous-option « Mode debug ») déclenche l'appel à l'Edge Function `post-to-facebook` ; le résultat (lien vers le post ou erreur) est affiché inline et la navigation vers `/actus` est suspendue jusqu'au retour. |
| `pages/TeamMatchesPage.tsx` | `/team-matches` | **Vue Grille de saison** (refonte). Trois vues en état local — **Grille** (défaut), **Agenda**, **Liste** — partageant le chargement de `useTeamSeasonGrid`, plus un **panneau latéral persistant** (372 px) en mode « rencontre » ou « affiche ». Saisir un score prend 2 clics (cellule → CTA du panneau). `view` et `saisonId` persistés par `useLocalStorage` sous une clé préfixée club (`tmc:${clubId}:team-matches:view`) ; rechargement au `focus` de la fenêtre pour que le retour depuis `/team-matches/rencontre/:id` montre le nouveau score. Barre de contexte : compteurs (scores à saisir / équipes en cours / rencontres ce week-end) + légende des états. Squelette de grille au chargement. **Ne porte plus** le bouton « Admin » (le référentiel s'atteint par la carte « Saisons & équipes » de l'accueil du BO, et par les liens des états vides « Aucune saison » / « Aucune compétition » / « Aucune équipe ») ni les créations d'équipe/compétition (migrées dans `TeamMatchesAdminPage`), et plus de `GeneratePosterModal` (remplacée par `PosterPanel`). |
| `pages/TeamMatchesAdminPage.tsx` | `/team-matches/admin` | Admin du référentiel : CRUD saisons (une seule active à la fois) + CRUD compétitions (nom/type/genre/catégorie/format, genre & catégorie filtrés selon le type) + **case à cocher « Terminée »** par compétition (`team_competitions.terminee` — sort le championnat de la grille active de `/team-matches`). **Section Équipes** : liste des équipes de la saison, création (`CreateEquipeForm`, génère les étapes `J1..JN`) et suppression — toutes deux **déplacées depuis `TeamMatchesPage`** lors de la refonte, faute de quoi il n'y aurait plus aucun moyen de créer ou supprimer une équipe. |
| `pages/TeamEquipePage.tsx` | `/team-matches/equipe/:id` | Détail d'une équipe : phase de poule (table des journées) + phase finale (table des stades). Bouton « Qualifier » (modale qualifiée/éliminée + stade de départ → génère les étapes de finale). |
| `pages/TeamRencontrePage.tsx` | `/team-matches/rencontre/:id` | Suivi d'une rencontre : matches individuels (`TeamMatchLineModal`), score (`TeamScoreSection`), photos (`TeamPhotosSection`). Bascule chaque match vers `live_matches` (mapping équipe 1 = `j1`+`j3` = club, équipe 2 = `j2`+`j4` = adverse, `type_tournoi` = « compétition — étape »). À l'ouverture : `syncFromLive()` requête les lives liés et met à jour `gagnant` + recalcule le score si un live est terminé (court-circuité si `rencontre.wo`). Bouton « Déclarer WO » (gagné/perdu par forfait) : fixe `wo=true` + score auto = `totalPointsFormat(format)` à 0 ; bouton « Annuler le WO » remet `wo=false` et score `null`. `TeamScoreSection` masquée quand `wo` (score affiché par le bandeau WO). |
| `components/teamMatches/TeamRencontreForm.tsx` | `/team-matches/rencontre/new`, `/team-matches/rencontre/:id/edit` | Formulaire création/édition d'une rencontre (club adverse, date/heure, lieu). Contexte (compétition/étape/équipe) affiché en lecture seule. |
| `pages/MembersPage.tsx` | `/admin/members` | **Multi-tenant (PR5-bis)** — gestion des membres du club par un **admin de ce club** (route `adminOnly` ; un `manager` n'y a pas accès). Liste : nom (`prenom nom`, « — » si le profil est encore vide), **email**, rôle en `<select>` inline, badge **« Invitation en attente »** (`auth.users.last_sign_in_at IS NULL`) et badge « Vous ». Actions par ligne : changement de rôle, **Retirer** (confirmation nommant la personne et le club ; détache du club, ne supprime pas le compte) et, sur une ligne en attente, **Renvoyer l'invitation**. Le `<select>` et « Retirer » sont désactivés (infobulle) sur le **dernier admin** — confort seulement, la barrière est le garde-fou serveur. Sous-composants locaux : `InvitePanel` (**contenu de l'ancienne `InvitePage`** : email + rôle, « Envoyer l'invitation » ou « Générer un lien à copier » ; une invitation réussie rafraîchit la liste) et `CopyLink` (panneau de copie partagé entre l'invitation et la relance). Tout passe par l'Edge Function `club-members` — aucune requête directe sur `club_members`. **Libellés** : le rôle « définit ce que la personne voit dans le back-office » — la RLS cloisonne par club, pas par rôle (`MULTI_TENANT.md` §4.1). |
| `pages/SuperAdminPage.tsx` | `/super-admin` | **Multi-tenant (PR5)** — console plateforme de provisioning, réservée au **super-admin** (`isSuperAdmin` seul : le super-admin n'est pas un rôle de club). Liste des clubs (slug, sport, statut, date de création, nombre de membres, marqueur **« aucun admin »** = aucun `club_members.role = 'admin'`, état d'un club fraîchement créé). Création d'un club (`name`, `slug` prérempli par slugification et éditable, `sport`) — un seul `INSERT` sur `clubs`, la ligne `club_settings` étant posée par le trigger `clubs_create_settings` ; le slug est validé côté front aux **mêmes règles que le CHECK `clubs_slug_format`** (helpers `slugify` / `validateSlug` locaux) et le conflit d'unicité `23505` a son message dédié. Suspension / réactivation (`status`) avec confirmation, **refusée sur le club courant** (le `ClubContext` filtre `status = 'active'` : on se couperait l'accès au BO ouvert). Invitation du 1ᵉʳ admin via `invokeInvite()` (`role: 'admin'`, envoi email **ou** lien à copier), sous-composant `InviteAdminPanel`. Bouton « Entrer dans ce club » → `enterSupportClub()`. |
| `pages/AcceptInvitePage.tsx` | `/accept-invite` | Page publique d'activation de compte. Supabase JS parse automatiquement le hash `#access_token=...&type=invite` (`detectSessionInUrl: true`). L'invité saisit son **prénom + nom** et son mot de passe ; à la validation, `supabase.auth.updateUser({ password })` puis `upsert` sur `profiles` (RLS `auth.uid() = id` autorise l'écriture de son propre profil), puis redirection vers `/`. |

### Composants

| Fichier | Utilisé dans | Rôle |
|---|---|---|
| `components/ConfigurationForm.tsx` | `TournamentPage` | Formulaire de configuration GlobalConfig |
| `components/ScheduleView.tsx` | `TournamentPage` | Affichage calendrier/tableau + drag-and-drop des matchs |
| `components/ConfigDropdown.tsx` | `TournamentPage` | Sélecteur de configurations prédéfinies |
| `components/EventCard.tsx` | `EventsPage` | Carte d'un événement dans la liste (badge type, dates, prix, actions) |
| `components/EventForm.tsx` | `EventsPage` / routes | Formulaire création/édition (voir ligne pages) |
| `components/LiveMatchCard.tsx` | `LiveScorePage` | Carte d'un match (V1 TV Board). Hero bar avec bloc court coloré contextuel (rouge LIVE / sombre Finished / ambre Pending), chips `type_tournoi` + Simple/Double, badge statut compact + menu kebab. Libellé du bouton primaire selon statut/ownership : Démarrer (pending) / Reprendre (live + own) / **Prendre le contrôle** (live + autre user, via prop `isOwnLive`) / Voir (finished) + Supprimer. Scoreboard ATP avec trophée vainqueur, classement (chip) et club par joueur, cellules de score 48×48 (set gagné fond sombre). Footer date + "Live · en cours" ou "Vainqueur : …". Badge "À supprimer" intégré au hero bar. |
| `components/LivePulse.tsx` | `LiveMatchCard`, `LiveScorePage` | Point rouge pulsant (animate-ping). Utilisé sur les indicateurs LIVE (badge header + statut carte). |
| `components/LiveScoreEntry.tsx` | `LiveMatchPage` | Interface +/- par joueur pour saisir le score (sets 1-3, tiebreak auto à 6/6, choix format set 3). Désactivé si `status != 'live'` ou si la prop `forceDisabled` est `true` (utilisée par `LiveMatchPage` lors d'une perte de contrôle live). |
| `components/MarkdownEditor.tsx` | `ActuForm`, `EventForm` | Éditeur Markdown contrôlé (`value` / `onChange`) avec onglets Écrire/Aperçu, toolbar (Gras, Italique, Souligné via `<u>`, H1/H2/H3) et raccourcis Ctrl/Cmd+B / +I. Aperçu : `react-markdown` + `remark-breaks` + `rehype-raw` (pour le souligné HTML inline). Embarque le helper `expandBlankLines`. |
| `components/TeamMatchImagePreview.tsx` | `teamMatches/poster/PosterPanel` | Rendu DOM de l'affiche 1414×2000 des rencontres à venir. Cellules Bandeau auto-adaptatives. Monté hors viewport et exporté en JPEG via `html-to-image`. |
| `components/teamMatches/teamMatchLabels.ts` | Pages/composants `teamMatches/` | Module helper (pas de React) du module Matches par équipe : libellés d'affichage (type/genre/catégorie/format/stade), contraintes genre/catégorie selon le type, `FORMAT_SPECS` (nb simples/doubles + points du double), helpers `competitionLabel` / `etapeLabel` / `stadesFromDepart` / `computeScore` / `totalPointsFormat` (total de points en jeu, utilisé pour le score d'un WO). |
| `components/teamMatches/TeamMatchesHeader.tsx` | Pages `teamMatches/` | Header partagé (back link + titre + sous-titre + actions + déconnexion). |
| `components/teamMatches/useTeamSeasonGrid.ts` | `TeamMatchesPage` | Hook de chargement agrégé de la saison : **un seul** `select` imbriqué `team_competitions → team_equipes → team_etapes → team_rencontres` (remplace la cascade séquentielle), filtré `club_id` + `saison_id`. Normalise la relation `team_rencontres` (`etape_id` est `UNIQUE`) en objet unique une fois pour toutes. C'est ici que vit toute la dérivation : colonnes de poule = `max(nb_journees_poule, plus grand numero_journee)` du **groupe** (la colonne est par équipe et mutable), une colonne par `stade_finale` réellement présent (`stadesFromDepart` crée les 5 stades d'un coup), cascade d'état de cellule (`wo` **avant** les scores), bilans et compteurs. Les `team_match_lines` ne sont pas chargées à ce niveau. |
| `components/teamMatches/gridTypes.ts` | vues + hook | Types de la grille (`CellState`, `Colonne`, `Cell`, `Bilan`, `CompetitionBlock`, `RencontreEntry`, `CellRef`), classes Tailwind par état (`CELL_CLASSES`), `bilanLabel` (segment `N` seulement si nuls > 0) et calcul de largeur (`gridMinWidth` / `gridTemplateColumns` — jamais de largeur codée en dur). |
| `components/teamMatches/weekend.ts` | compteurs, `AgendaView`, `PosterPanel` | **Définition unique** du week-end : vendredi 18:00 → dimanche 23:59:59.999, heure locale. `currentWeekendRange` / `nextWeekendRange` / `lastWeekendRange` + `isInRange`, `formatWeekendLabel`, `weekendFileDate`. Le décalage ±1 semaine se fait en **jours calendaires** : en millisecondes le vendredi dérive d'une heure au passage à l'heure d'été. |
| `components/teamMatches/SeasonGridView.tsx` | `TeamMatchesPage` | Vue Grille : un bloc par compétition, lignes = équipes, colonnes = `J1…Jn` puis les stades de finale (en-têtes ambrés, séparateur à gauche du premier). Colonne équipe `sticky left-0`, scroll horizontal plutôt que compression. Cellule `na` = `<div>` inerte hachuré (pas un bouton) ; section repliable « Championnats terminés (n) ». |
| `components/teamMatches/AgendaView.tsx` | `TeamMatchesPage` | Vue Agenda : À saisir / Ce week-end / Week-end suivant / Déjà jouées, bornes issues de `weekend.ts`, section vide masquée. Le clic sélectionne la rencontre dans le panneau sans changer de vue. |
| `components/teamMatches/ListeView.tsx` | `TeamMatchesPage` | Vue Liste : une ligne par équipe, pastilles d'avancement (mêmes états que la grille, `na` inclus), bilan, état (phase en cours ou issue finale), bouton `Ouvrir` → bascule en Grille sur l'équipe. |
| `components/teamMatches/RencontrePanel.tsx` | `TeamMatchesPage` | Panneau latéral, mode « rencontre » : contexte, score, `team_match_lines` chargées **à la sélection** (requête annulable, sans cache), CTA contextuel et actions secondaires. Les actions secondaires ouvrent `/team-matches/rencontre/:id`, seule surface d'édition de ces flux ; « Créer une actu » navigue vers `/actus/new` avec le même `location.state` que `TeamPhotosSection`. |
| `components/teamMatches/poster/posterHelpers.ts` | `PosterPanel` | `RencontreWithContext`, `MAX_POSTER_MATCHES = 8`, `formatRencontreDate`, `rencontreToTeamMatch` — **extraits tels quels** de `TeamMatchesPage`. Le mapping porte des fallbacks métier (`60_ans → 'Seniors +35'`, `teamNumber = min(numero, 3)`) dont dépend le rendu de `TeamMatchImagePreview`. |
| `components/teamMatches/poster/PosterPanel.tsx` | `TeamMatchesPage` | Panneau latéral, mode « affiche » — **remplace `GeneratePosterModal`**. Consomme les rencontres déjà chargées par le hook (plus de requête dédiée), groupées par week-end avec « Tout prendre ». Présélection = le week-end courant tronqué à 8. `toJpeg(node, { quality: 0.92, pixelRatio: 2 })` déclenché **par le bouton uniquement** (pas à chaque coche), téléchargement `affiche-rencontres-YYYY-MM-DD.jpg` (samedi du week-end). |
| `components/teamMatches/TeamMatchLineModal.tsx` | `TeamRencontrePage` | Modale de saisie d'un match individuel (simple/double, joueurs club + adverses, prénom/nom/classement). |
| `components/teamMatches/TeamMatchScoreModal.tsx` | `TeamRencontrePage` | Modale de saisie du résultat d'un match **sans live** : vainqueur (club/adverse) + score libre optionnel. Met à jour `team_match_lines.gagnant` / `.score`. |
| `components/teamMatches/TeamScoreSection.tsx` | `TeamRencontrePage` | Section score final : saisie manuelle (2 inputs) si aucun match n'a de vainqueur, sinon score calculé depuis les `gagnant` (live **ou** manuels) + bouton « Recalculer ». |
| `components/teamMatches/TeamPhotosSection.tsx` | `TeamRencontrePage` | Upload multiple de photos (bucket `team-match-photos`, max 10 Mo), grille supprimable, bouton « Créer une actu » (navigation `/actus/new` avec `location.state = { titre, image_urls }`). |

### Infra

| Fichier | Rôle |
|---|---|
| `hooks/useLocalStorage.ts` | Hook générique `useLocalStorage<T>(key, defaultValue)`. Consommé par `TeamMatchesPage` pour persister la vue et la saison sous une clé **préfixée club** (`tmc:${clubId}:team-matches:…`). La persistance TMC, elle, se fait dans la table Supabase `tournaments`, pas en localStorage. |
| `lib/supabase.ts` | Client Supabase (auth uniquement, pas de base de données utilisée) |
| `lib/functions.ts` | **Multi-tenant (PR5-bis)** — `invokeEdgeFunction<T>(name, body, fallbackError?)` + type `EdgeResult<T>`. Porte le parsing d'erreur non trivial des Edge Functions maison (sur une réponse non-2xx, `invokeErr.message` est générique — le vrai message français est dans le body), **extrait de `lib/invite.ts`** quand `club-members` a eu besoin du même traitement : à écrire une seule fois. |
| `lib/invite.ts` | **Multi-tenant (PR5)** — `invokeInvite(body)`, appel de l'Edge Function `invite-user` partagé entre le panneau d'invitation de `MembersPage` et `SuperAdminPage`. **PR5-bis** : mince wrapper au-dessus de `invokeEdgeFunction` (comportement inchangé). Contrat PR4 : `{ email, redirectTo, action, club_id, role }`. |
| `lib/clubMembers.ts` | **Multi-tenant (PR5-bis)** — appels typés de l'Edge Function `club-members` : `listClubMembers(clubId)`, `setClubMemberRole(clubId, userId, role)`, `removeClubMember(clubId, userId)`, `resendClubInvite(clubId, userId, redirectTo)`, + le type `ClubMember` (`user_id, email, prenom, nom, role, created_at, status`). `resendClubInvite` renvoie `email_sent` : c'est lui qui dit si un email est réellement parti ou si seul un lien a pu être régénéré. |
| `lib/storage.ts` | **Multi-tenant (PR6a)** — source unique des chemins Supabase Storage. Expose `STORAGE_BUCKETS` (les 4 buckets : `actu-images`, `event-images`, `team-match-photos`, `content-images`), `sanitizeFilename(name)`, `clubPath(clubId, ...segments)` et `extractStoragePath(bucket, publicUrl)`. **Mutualise 5 copies identiques** d'`extractStoragePath` (`ActuForm`, `EventForm`, `TeamPhotosSection`, `EventsPage`, `ActusPage`) et 4 de `sanitizeFilename` — même geste que l'extraction de `lib/functions.ts` en PR5-bis : changer la convention de chemins sans mutualiser d'abord, c'était 5 occasions de diverger. **Convention D12** : premier segment = `club_id`, le reste inchangé (`actu-images/<club_id>/<actuId>/<ts>-<i>-<nom>`, `content-images/<club_id>/inline/<ts>-<nom>`…). Les policies Storage lisent ce 1ᵉʳ segment : un chemin construit hors de ce module est refusé par la RLS. `clubPath` **lève** si `clubId` est vide (impossible en pratique, cf. garde `ClubContext.tsx:110`) plutôt que d'uploader sous `null/…`, qui passerait la clause legacy sans que personne le voie. `extractStoragePath` rend le chemin **tel qu'il est stocké** — avec préfixe pour les objets post-PR6a, sans pour les objets legacy, que la clause « grandfather » des policies couvre. |
| `lib/clubConfig.ts` | **Multi-tenant (PR6a)** — contrat de `club_settings.config` (`MULTI_TENANT.md` §6.1). Schéma **zod** + `defaultClubConfig()` + `parseClubConfig(raw)` + `CLUB_CONFIG_VERSION`. Aucun import React ni Supabase : déplaçable tel quel dans le paquet partagé avec `web/` (PR9). **La lecture ne jette jamais** — `safeParse`, `.catch()` par feuille (une clé au mauvais type retombe sur son défaut sans invalider ses voisines ; une **liste** retombe sur `[]` en bloc dès qu'une entrée est malformée), et les **clés inconnues sont préservées** pour qu'un BO en retard d'une version n'efface pas un groupe qu'il ne connaît pas. `config = '{}'` est le cas **nominal** (club provisionné par la console) et rend l'arbre de défauts complet. Groupes couverts : **`brand`, `home`, `contact`** seulement — les autres suivront leurs consommateurs. `brand.color` a un défaut **vide** volontairement : les couleurs en dur du BO/PWA (`#C8102E`) restent la propriété de PR7-bis. La validation **stricte** (écriture) est PR6b. |
| `hooks/useClubConfig.ts` | **Multi-tenant (PR6a)** — `useClubConfig()` → `{ config, loading, reload }`. Lit `club_settings.config` du `clubId` courant et le passe à `parseClubConfig` ; retombe sur les défauts en cas d'erreur plutôt que de casser l'écran. Hook autonome **et non un provider dans `ClubContext`** : ce dernier est dupliqué avec la copie PWA (« garder les deux synchronisés »), or la PWA n'a aucun usage de la config — l'y greffer forcerait une divergence ou un changement PWA gratuit. À promouvoir en provider si plusieurs écrans le montent en parallèle (PR6b), pas avant. |
| `contexts/ClubContext.tsx` | **Multi-tenant (PR2)** — `ClubProvider` + hook `useClub()`. Résout le club courant (tenant) depuis le hostname (`<slug>.feelike.app`, sinon `VITE_DEV_CLUB_SLUG`, fallback `cac-tennis`) via un `select` sur `clubs`, expose `{ clubId, club, loading }`. Toutes les requêtes métier filtrent par `clubId` (`.eq('club_id', clubId)` / `club_id: clubId`). **PR3** : si la résolution échoue (slug **et** fallback introuvables), le provider rend un écran bloquant « Club introuvable ou indisponible » **au lieu de ses enfants** — l'app n'est jamais montée avec `clubId === null`, cas qui écrivait auparavant `club_id: null` en silence (désormais rejeté par le NOT NULL + la RLS `tenant_isolation`). La vraie page « club inconnu / suspendu » reste du ressort de PR13. Dupliqué en `pwa/src/contexts/ClubContext.tsx` (comme `liveScoreRules.ts`). **PR5** : ordre de résolution **override de support → hostname → fallback dev**. L'override est la clé `localStorage` `feelike_support_club` (constante `SUPPORT_CLUB_KEY`) contenant un `clubs.id`, posée par `enterSupportClub(id)` et effacée par `exitSupportClub()` (toutes deux exportées ici, toutes deux rechargent l'app sur `/`) ; il est résolu **sans le filtre `status = 'active'`** — entrer dans un club suspendu pour le diagnostiquer est l'usage même du support — et un id introuvable lève l'override au lieu de bloquer le BO. Le contexte expose `isSupport` (bandeau d'App.tsx). ⚠️ **Divergence volontaire avec la copie PWA** : l'override est BO uniquement. |
| `contexts/ClubRoleContext.tsx` | **Multi-tenant (PR4)** — `ClubRoleProvider` + hook `useClubRole()` → `{ role, isMember, isSuperAdmin, loading }`. Lit `club_members.role` de `auth.uid()` sur le `clubId` du `ClubContext` (policy `club_members_select_own`) + `profiles.is_super_admin`, en **deux requêtes lancées une seule fois** depuis le provider monté au-dessus des routes (jamais un `useEffect` par consommateur). Re-évalué sur `onAuthStateChange`, dédupliqué par clé `user:club` pour ne pas refetcher à chaque `TOKEN_REFRESHED`. `isMember = role !== null` ; un super-admin **non membre** a `isMember === false` mais un rôle effectif `admin` côté UI (accès support). Masquage UI **uniquement** — la RLS reste la source de vérité (cf. `MULTI_TENANT.md` §4). Pas de copie PWA (hors périmètre PR4). |
| `App.tsx` | Routeur React Router + guard d'authentification, englobé dans `<ClubProvider>` (attend `club loading === false` avant de rendre les routes) puis `<ClubRoleProvider>` (monté **après** résolution de l'auth et du club). **PR4** — `GuardedRoutes` ajoute : (1) la **garde d'appartenance** — un compte authentifié qui n'est ni membre du club résolu ni super-admin obtient l'écran `NoClubAccess` (refus plein page nommant le club + bouton « Se déconnecter », pas de redirection vers `/login` : la session est bonne, c'est le club qui ne l'est pas) au lieu des routes ; `UNGUARDED_PATHS` (`/login`, `/accept-invite`) reste atteignable ; (2) la **garde de route** `adminOnly` sur `/admin/members` (redirection vers `/` — l'URL est devinable même carte masquée ; **PR5-bis** : la route était `/admin/invite`, qui redirige désormais en permanence vers `/admin/members` — l'URL est documentée dans le README et peut traîner dans un favori). **PR5** — garde `superAdminOnly` sur `/super-admin` (`isSuperAdmin` **seul**, jamais `role === 'admin'` : c'est une surface plateforme) + composant `SupportBanner`, bandeau collant rendu au-dessus des `<Routes>` tant que `useClub().isSupport` est vrai (« Support — vous consultez *&lt;club&gt;* (suspendu) » + bouton **Quitter** → `exitSupportClub()`). |
| `main.tsx` | Point d'entrée, monte `<BrowserRouter>` |

---

## Flux de données — TMC Planner

```
GlobalConfig (formulaire)
  → tmcLogic.generateTMCMatches() par TournamentConfig  →  Match[][]
  → scheduler.generateSchedule(config, matchesByTournament)  →  Schedule
  → ScheduleView (affichage)
  → moveMatch / moveMatches (drag-and-drop)  →  Schedule mis à jour
  → useLocalStorage (persistance)
```

## Flux de données — Programmation Image

```
PDF (pdfjs-dist) ou CSV (texte brut)
  → parsing dans ProgrammationImagePage
  → état local React (liste de matchs du jour)
  → rendu DOM → html-to-image → export JPEG
```

---

## Constantes importantes

- `MIN_HOURS_BETWEEN_MATCHES = 240 min` (4h) — délai obligatoire entre deux tours du même tournoi
- Nombre de joueurs supportés : 4, 8, 16 (puissances de 2) + 12 et 24 (tableaux asymétriques)
- Capacité max affiche : 8 matchs par page A4

---

## Specs fonctionnelles

Voir `docs/specs/` :
- `SCHEDULING_RULES.md` — règles de l'algo de planification (contraintes R1–R5)
- `GEN_PROG.md` — spec du module Programmation Image
- `GEN_PROG_TO_LIVE_SCORE.md` — basculement des matchs détectés depuis l'affiche vers Live Score (création en bloc dans `live_matches`)
- `GEN_PROG_WO_FILTER.md` — exclusion des matchs walkover (WO) du rendu de l'affiche et du transfert Live Score (flag `wo` sur le `Match` local, détection PDF dans la slice de colonne + colonne CSV optionnelle)
- `EVENTS.md` — spec du module Events (table Supabase `events`, bucket `event-images`, flux JSON)
- `LIVE_SCORE.md` — spec du module Live Score (table Supabase `live_matches`, règles de score, UI back-office, préparation Realtime)
- `ACTUS.md` — spec du module Actus (table Supabase `actus`, bucket `actu-images`, multi-images, brouillon/publié, lecture `anon` PWA)
- `ACTUS_FOCAL_POINT.md` — point de focus par image d'actu (colonne `image_focal_points`, overlay BO, helper CSS PWA `objectPosition`)
- `ACTUS_FACEBOOK.md` — publication simultanée d'une actu sur la page Facebook du club via Edge Function `post-to-facebook` (checkboxes dans `ActuForm`, secrets `FACEBOOK_PAGE_ID` / `FACEBOOK_PAGE_ACCESS_TOKEN`)
- `TEAM_MATCHES.md` — module Matches par équipe (référentiel saisons/compétitions/équipes/étapes, rencontres, matches individuels, bascule Live Score, photos → actu)
- `PWA_PULL_TO_REFRESH.md` — pull-to-refresh PWA sur Actus & Événements (`usePullToRefresh` + `PullToRefreshWrapper`)

## Edge Functions Supabase

| Fonction | Rôle |
|---|---|
| `supabase/functions/post-to-facebook/index.ts` | Publie une actu sur la page Facebook du club. Appelée depuis `ActuForm` quand la case « Publier aussi sur Facebook » est cochée. Utilise les secrets `FACEBOOK_PAGE_ID` et `FACEBOOK_PAGE_ACCESS_TOKEN`. |
| `supabase/functions/invite-user/index.ts` | Envoie une invitation par email à un nouvel utilisateur via `auth.admin.inviteUserByEmail`, ou — si `action: 'generate-link'` — retourne juste le lien d'invitation via `auth.admin.generateLink` sans envoyer d'email (fallback rate limit). Le `redirectTo` est fourni par le client (`window.location.origin + '/accept-invite'`) — la whitelist Auth → URL Configuration côté dashboard Supabase fait foi pour la sécurité (**à étendre aux origins `*.feelike.app`**). Appelée depuis `InvitePage`. **Contrat PR4** : body `{ email, redirectTo, action, club_id, role }` — `club_id` et `role` **obligatoires**, `role ∈ ('admin','manager','member')` validé côté serveur. **Autorisation** : au-delà du JWT, la function lit en *service role* `club_members` du caller sur le `club_id` demandé + `profiles.is_super_admin`, et exige `role = 'admin'` sur **ce** club ou super-admin — sinon `403` (un `manager` n'invite personne ; c'est aussi ce contrôle qui empêche d'injecter le `club_id` d'un tiers). Puis invitation → `data.user.id` → **upsert `club_members (club_id, user_id, role)`** `ON CONFLICT (club_id,user_id)` : l'appartenance existe dès l'émission, `AcceptInvitePage` n'a rien à créer. Un échec d'upsert remonte en `500` (un compte sans appartenance est le bug corrigé). Cas `email_exists` (compte déjà existant, invité sur un 2ᵉ club) : la function retrouve l'`user_id` via l'endpoint admin `GET /auth/v1/admin/users?filter=<email>` (recherche partielle → égalité stricte revérifiée), rattache le compte et répond `{ success: true, already_existed: true }` **sans envoyer d'email**. ⚠️ Déploiement **manuel** : `supabase functions deploy invite-user`. |
| `supabase/functions/club-members/index.ts` | **Multi-tenant (PR5-bis)** — gestion des membres d'un club en *service role*. `POST` `{ club_id, action: 'list'|'set-role'|'remove'|'resend-invite', user_id?, role?, redirectTo? }`. **Autorisation** : même patron que `invite-user` — JWT → `callerId`, puis lecture service role de `club_members` (rôle sur le `club_id` **demandé**) + `profiles.is_super_admin` → `403` si ni admin de ce club ni super-admin ; le `club_id` envoyé par le front ne fait pas foi. **`list`** : `club_members` + `profiles` (2 requêtes — pas de FK `club_members → profiles`, PostgREST ne sait pas embarquer) + `auth.admin.getUserById` **par membre** pour l'email et `last_sign_in_at` (les ids sont connus : plus juste que paginer les comptes de *tous* les clubs) → `status: 'active'|'pending'`, tri admins d'abord puis email, **`is_super_admin` jamais exposé** (information plateforme). **`set-role`** / **`remove`** : la cible doit être membre de **ce** club, puis garde-fou **« au moins un admin »** (refus serveur si la cible est le dernier `admin`) ; `remove` fait un `DELETE` sur `club_members` seul — ni `auth.users` ni `profiles`. **`resend-invite`** : refusé (`400`) sur un membre `active` ; rappeler `invite-user` ne marcherait pas (branche `already_existed` → « succès » sans email), donc `inviteUserByEmail` en direct — GoTrue ne lève `email_exists` que sur un compte **confirmé**, un invité jamais activé reçoit donc bien un nouvel email — avec repli `generateLink` (`invite` puis `recovery`) ; la réponse porte **`email_sent`**, c'est elle qui dit à l'UI si un email est parti ou si seul un lien a été régénéré. Prologue (CORS, auth, `jsonResponse`) **dupliqué** depuis `invite-user` : les functions Deno sont indépendantes, c'est le patron maison. ⚠️ Déploiement **manuel** : `supabase functions deploy club-members`. |

## Infrastructure Supabase

- Table `events` + bucket `event-images` : migration `supabase/migrations/20260418_events.sql` puis `supabase/migrations/20260515_event_team_matches.sql` (ajout colonne `team_matches JSONB`)
- Table `live_matches` (+ enums + trigger updated_at réutilisé + Realtime) : migration `supabase/migrations/20260423_live_matches.sql` puis `supabase/migrations/20260519_live_matches_court.sql` (ajout colonne `court TEXT` nullable — terrain saisi au démarrage du live) puis `supabase/migrations/2026052301_live_matches_started_at.sql` (ajout colonne `started_at TIMESTAMPTZ` nullable — horodatage du passage à `live`, sert au tri de la section "En live") puis `supabase/migrations/2026052302_live_matches_retired_player.sql` (ajout colonne `retired_player live_match_winner` nullable — renseignée en cas d'abandon, badge "Abandon" sur les cartes BO/PWA)
- Table `profiles` (id/prenom/nom, FK `auth.users(id)`, RLS lecture libre `authenticated` + `anon`, écriture sur son propre profil, trigger `on_auth_user_created` qui crée un profil vide à chaque nouveau user) : migration `supabase/migrations/20260521_profiles.sql`. Utilisée pour afficher le nom du gestionnaire actuel dans le warning *« Prendre le contrôle »* (BO + PWA). Les profils existants sont à insérer manuellement après application de la migration. |
- Table `actus` (`image_urls TEXT[]`, `image_focal_points JSONB`, `image_captions TEXT[]`, `published`, `published_at`) + bucket `actu-images` + RLS `anon`/`authenticated` : migrations `supabase/migrations/2026042601_actus.sql` puis `supabase/migrations/2026042602_actus_image_urls.sql` (patch `image_url` → `image_urls`) puis `supabase/migrations/20260506_actus_focal_points.sql` (ajout `image_focal_points` parallèle à `image_urls`) puis `supabase/migrations/20260510_actus_image_captions.sql` (ajout `image_captions` — légendes Facebook par photo, BO-only)
- Policies RLS `anon` (lecture publique pour PWA) : à ajouter sur `events` et `live_matches` (voir `docs/specs/PWA.MD`). Déjà en place sur `actus`.
- GRANTs explicites par rôle sur les 4 tables existantes (`events`, `live_matches`, `actus`, `profiles`) : migration `supabase/migrations/20260603_grant_public_tables.sql`. Voir section *Migrations Supabase* ci-dessous.
- Module **Matches par équipe** : tables `team_saisons`, `team_competitions`, `team_equipes`, `team_etapes`, `team_rencontres` (trigger `updated_at` réutilisant `set_updated_at()`), `team_match_lines` (`joueurs_club`/`joueurs_adverse` en JSONB, `live_match_id` FK → `live_matches`) + bucket public `team-match-photos` : migration `supabase/migrations/20260606_team_matches.sql`. RLS `authenticated` ALL sur chaque table. Aucune migration depuis `events`. Exposition lecture `anon` (PWA Match équipes) ajoutée par `supabase/migrations/20260611_team_matches_pwa_read.sql` sur `team_saisons`/`team_competitions`/`team_equipes`/`team_etapes`/`team_rencontres` uniquement — **`team_match_lines` (joueurs nominatifs) reste back-office only**. Colonne `wo boolean NOT NULL DEFAULT false` ajoutée à `team_rencontres` par `supabase/migrations/20260628_team_rencontres_wo.sql` (déclaration d'une rencontre gagnée/perdue par forfait au niveau rencontre ; pas de nouvelle policy).
- **Multi-tenant — socle (PR1)** : migration `supabase/migrations/20260629_multi_tenant_socle.sql` (expand, non-bloquante — cf. `docs/specs/MULTI_TENANT.md`). Crée l'enum `club_role ('admin','manager','member')` et les tables socle `clubs` (`slug` unique, `name`, `sport`, `status`, `custom_domain`, trigger `updated_at`, RLS lecture `anon`+`authenticated` pour la résolution du tenant par sous-domaine), `club_members` (`club_id`/`user_id`/`role`, unique `(club_id,user_id)`, RLS SELECT own only), `club_settings` (`club_id` PK, `config` JSONB, trigger `updated_at`, RLS SELECT `authenticated`). Ajoute le flag `profiles.is_super_admin BOOLEAN DEFAULT false`. Ajoute une colonne `club_id UUID` **nullable, sans FK** sur les tables métier `events`/`live_matches`/`actus`/`team_saisons`/`team_competitions`/`team_equipes`/`team_etapes`/`team_rencontres`/`team_match_lines` (`profiles` exclu : appartenance via `club_members`). Insère le club #1 **CAC Tennis** (`slug='cac-tennis'`) + sa ligne `club_settings` vide, puis **backfille** `club_id` de toutes les lignes existantes avec l'id CAC. ⚠️ Le verrouillage (`club_id NOT NULL` + FK, RLS multi-tenant `tenant_isolation` + GRANTs, helpers `auth_club_ids()`/`auth.is_super_admin()`) et le seeding des `club_members` existants sont reportés à **PR3**.
- **Multi-tenant — `tournaments` + ClubContext (PR2)** : migration `supabase/migrations/20260718_tournaments_club_id.sql`. La table `tournaments` (persistance du TMC Planner : `id`, `user_id` FK→`auth.users` ON DELETE CASCADE, `config` JSONB, `schedule` JSONB nullable, `created_at`) avait été créée **à la main via le dashboard sur PROD** et n'existait sur **aucune** base reconstruite depuis les migrations (dev inclus). Cette migration la **met sous gestion des migrations** : `CREATE TABLE IF NOT EXISTS` répliquant le schéma PROD (+ RLS `ENABLE` + policy socle `"authenticated users see all"` `FOR ALL TO public USING (auth.uid() IS NOT NULL)` + `GRANT ALL` à `anon`/`authenticated`/`service_role`), puis `ALTER … ADD COLUMN IF NOT EXISTS club_id UUID` (nullable, sans FK, patron PR1) + backfill CAC. Idempotent : no-op sur PROD (table déjà là), création complète sur dev. Lue/écrite par `HomePage.tsx` + `TournamentPage.tsx`. La RLS `tenant_isolation` par `club_id` reste reportée à **PR3**. Côté applicatif, PR2 ajoute `ClubContext` (BO + PWA, cf. `src/contexts/ClubContext.tsx`) et fait passer `.eq('club_id', clubId)` / `club_id: clubId` à **toutes** les requêtes des 9 tables métier + `tournaments`. `profiles` reste hors périmètre (appartenance via `club_members`). Cas particulier : l'activation d'une saison (`TeamMatchesAdminPage`) filtre le `.update({actif:false}).neq('id',…)` par `club_id` pour ne pas fuiter entre clubs. **Aucune RLS active en PR2** — filtrage = garde applicative, la sécurité arrive en **PR3**.
- **Multi-tenant — verrouillage RLS (PR3)** : migration `supabase/migrations/20260816_multi_tenant_rls.sql`. C'est le « flip the switch » de l'isolation, idempotente. Contenu : (1) helpers `public.auth_club_ids()` (SETOF uuid — clubs de `auth.uid()` via `club_members`) et `public.is_super_admin()` (lit `profiles.is_super_admin`), tous deux `STABLE SECURITY DEFINER SET search_path = public` pour éviter la récursion RLS, `GRANT EXECUTE` à `authenticated` ; (2) garde-fou qui **échoue bruyamment** si un `club_id IS NULL` subsiste sur l'une des 10 tables ; (3) **seeding `club_members`** — tout compte `auth.users` devient `club_members(CAC, 'admin')` (étape reportée de PR1 : sans elle `auth_club_ids()` est vide et la RLS verrouille tous les comptes dehors) ; (4) `club_id` **NOT NULL + FK → clubs(id)** + index `idx_<table>_club_id` sur les 10 tables métier (`events`, `live_matches`, `actus`, `tournaments`, `team_saisons`, `team_competitions`, `team_equipes`, `team_etapes`, `team_rencontres`, `team_match_lines`) ; (5) DROP des policies larges `authenticated USING (true)` + `REVOKE ALL ON tournaments FROM anon` ; (6) policy **`tenant_isolation`** `FOR ALL TO authenticated USING/WITH CHECK (club_id IN (SELECT public.auth_club_ids()) OR public.is_super_admin())` sur les 10 tables ; (7) garde-fou final qui échoue s'il reste une policy `authenticated`/`public` autre que `tenant_isolation` sur ces tables (rattrape les policies créées à la main dans le dashboard). **Conservées** : policies `anon` (`actus_anon_select`, `team_*_anon_select`), policies `profiles`, policies Storage (scoping Storage = Phase 3 / D12). ⚠️ `events` et `live_matches` n'ont **aucune policy `anon` dans les migrations** alors que la PWA les lit publiquement : elles existent uniquement en prod, créées via le dashboard — requête de pré-vol sur `pg_policies` en en-tête de la migration à passer avant toute application.

- **Multi-tenant — rôles & invitations (PR4)** : **aucune migration de schéma** — `club_members` (PR1) et ses policies suffisent. Deux points d'exploitation : (1) le script **manuel** `supabase/scripts/pr4_assign_club_roles.sql` (⚠️ *pas* une migration, à exécuter à la main dans le SQL Editor, dev puis prod) attribue les rôles réels sur CAC — sans lui, le seeding PR3 laisse **tout le monde `admin`** et le masquage UI ne masque rien ; il embarque la requête de listing (`club_members` ⋈ `auth.users` ⋈ `profiles`) et un garde-fou « au moins un admin restant » ; (2) les écritures sur `club_members` passent **exclusivement par l'Edge Function `invite-user`** en service role — `club_members` n'a toujours qu'une policy `SELECT own`, aucune écriture client n'est possible (et ne doit pas être ajoutée à la main dans le dashboard). L'écran de **gestion des membres** (lister / changer un rôle / retirer) est reporté à **PR5-bis** (décidé en PR5 : c'est un écran d'administration des comptes côté **admin de club**, pas une fonction de la console de provisioning). ⚠️ *Corrigé en PR5-bis* : cette entrée annonçait « ouvrir `club_members` en lecture club-scopée + écritures ». Ce n'est **pas** ce qui a été fait — l'email et le statut « invitation en attente » vivent dans `auth.users`, hors de portée de la clé anon : le service role étant de toute façon nécessaire, PR5-bis passe **tout** par l'Edge Function `club-members` et `club_members` garde exactement ses deux policies `SELECT`.

- **Multi-tenant — correctif de sécurité `profiles` (PR5 §0)** : migration `supabase/migrations/2026081801_profiles_column_grants.sql`, **isolable** (ne touche que `profiles`, applicable seule en prod). `profiles.is_super_admin` (PR1) était **auto-attribuable** : `profiles_update_own` n'avait pas de `WITH CHECK` et les `GRANT INSERT/UPDATE` de `20260603` ne listaient aucune colonne — n'importe quel compte authentifié (la clé anon est publique dans les bundles BO et PWA) pouvait `PATCH /rest/v1/profiles?id=eq.<son_id> {"is_super_admin":true}` et court-circuiter `tenant_isolation` sur les 10 tables métier de **tous** les clubs. Correctif : `REVOKE INSERT, UPDATE` puis **`GRANT INSERT (id, prenom, nom)` / `GRANT UPDATE (id, prenom, nom)`** — le grant **par colonne** est la vraie barrière, la RLS ne sait pas restreindre une colonne — et `profiles_update_own` recréée avec `WITH CHECK (auth.uid() = id)`. La branche **INSERT** est verrouillée elle aussi (hors brief) : un compte **sans** ligne `profiles` — cas des comptes antérieurs au trigger `on_auth_user_created`, à insérer à la main — pouvait sinon s'insérer directement avec le flag. `id` reste dans les deux grants car `AcceptInvitePage` fait un `profiles.upsert({id, prenom, nom})`, que PostgREST traduit en `INSERT … ON CONFLICT DO UPDATE SET id = excluded.id, …` ; c'est inoffensif, les policies interdisent de poser `id` sur autre chose que soi-même. **Le flag ne se pose plus que via le SQL Editor / le service role** (bootstrap documenté dans le `README.md`).
- **Multi-tenant — console super-admin (PR5)** : migration `supabase/migrations/2026081802_super_admin_console.sql`, idempotente. (1) Garde-fou qui **nomme** les slugs existants incompatibles avant l'`ALTER TABLE` ; (2) **`CHECK clubs_slug_format`** — le slug est devenu une adresse DNS (D9) : `^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`, 2–32 caractères, **préfixe `app-` interdit** (`app-<slug>.feelike.app` est réservé à la PWA : un club `app-tennis` collisionnerait avec la PWA du club `tennis`) et slugs réservés `admin, app, www, api, mail, static, assets, feelike` — mêmes règles répliquées côté front, le CHECK est le filet ; (3) policies **`clubs_insert_super_admin`** / **`clubs_update_super_admin`** et **`club_settings_insert/update_super_admin`** (`public.is_super_admin()`, helper PR3) + `GRANT INSERT, UPDATE … TO authenticated` — sans grant, PostgREST refuse l'écriture quelle que soit la RLS, PR1 n'avait posé que `GRANT SELECT`. **Aucune policy `DELETE`** : on suspend un club, on ne le supprime pas (cascade sur 10 tables) ; les `SELECT` de PR1 sont intactes ; (4) policy **`club_members_select_super_admin`** (lecture seule — les écritures restent l'exclusivité de l'Edge Function `invite-user` en service role), sans laquelle la console ne peut ni compter les membres ni signaler « aucun admin » ; `club_members_select_own` conservée ; (5) trigger **`clubs_create_settings`** `AFTER INSERT ON clubs` (fonction `public.create_club_settings()`, `SECURITY DEFINER`) → `INSERT INTO club_settings (club_id) … ON CONFLICT DO NOTHING` : l'invariant « un club a toujours sa ligne `club_settings` » est porté par la base, pas par deux inserts côté front dont l'un peut échouer. **Aucune Edge Function nouvelle** : créer/suspendre un club ne manipule aucun secret (brief §4).
- **Multi-tenant — config & Storage (PR6a)** : migration `supabase/migrations/20260822_config_storage_tenant.sql`, idempotente. Ferme **deux** dettes. (1) **`club_settings`** : la policy PR1 `club_settings_select_authenticated USING (true)` — tout compte authentifié lisait la config de *tous* les clubs — est remplacée par **`club_settings_select_tenant`** (patron `tenant_isolation` : `club_id IN (SELECT public.auth_club_ids()) OR public.is_super_admin()`), et **`club_settings_update_club_admin`** ouvre l'écriture à l'**admin du club** via un `EXISTS` explicite sur `club_members` (le helper `auth_club_role()` que réclame la dette §4.1 n'est **pas** posé ici : un helper de rôle en base pour un seul appelant déborde d'une PR dont le sujet est l'isolation *par club*). Les policies super-admin de PR5 sont **conservées** ; aucune policy `DELETE` (une ligne `club_settings` vit et meurt avec son club) ; aucune lecture `anon` (PR9 ouvrira les clés publiques). Garde-fou `pg_policies` façon PR3, qui **lève** s'il subsiste une policy non cloisonnée. (2) **Storage (D12)** — c'était la **dernière fuite entre clubs** : les policies d'écriture des 4 buckets étaient scopées au bucket, pas au club. Le bucket **`content-images`**, créé au dashboard et versionné nulle part, est **rapatrié** (`INSERT … ON CONFLICT DO NOTHING` + policy de lecture publique). Les anciennes policies d'écriture sont purgées **par leur définition et non par leur nom** (boucle sur `pg_policies`) — celles de `content-images` portent des noms inconnus du dépôt ; les policies `SELECT` sont épargnées, la **lecture publique est inchangée** (vitrine + PWA déconnectée). 12 nouvelles policies (4 buckets × INSERT/UPDATE/DELETE) délèguent à **`public.can_write_club_object(name)`** : 1ᵉʳ segment ∈ `auth_club_ids()`, **ou** `is_super_admin()` (indispensable — tant que PR13 n'a pas posé le wildcard, l'override de support est le seul moyen d'atteindre un 2ᵉ club, et un super-admin en support n'est pas membre du club visité), **ou** la clause legacy ci-dessous. ⚠️ **Aucun cast `::uuid` sur `storage.foldername`** : `(storage.foldername(name))[1]` est du `text` et un objet legacy a un 1ᵉʳ segment non-UUID (`inline`, un id d'actu) — un cast ferait échouer la requête entière (`22P02`) alors qu'une policy doit renvoyer *faux*, pas lever. `UPDATE` inclus : aucun `.upload()` du BO n'utilise `upsert: true` aujourd'hui, mais le pré-vol dev a montré qu'une policy UPDATE existait en base (famille **`cac_buckets_*`** créée au dashboard — INSERT/UPDATE/DELETE sur les 4 buckets d'un coup, plus un SELECT public — **inconnue du dépôt**, et que seule une purge par *définition* pouvait attraper : un `DROP … IF EXISTS` par nom l'aurait laissée en place et la PR aurait été décorative). Ne pas la recréer aurait retiré une capacité au lieu de la cloisonner, et PR7 (background) comme PR7-bis (logos) voudront des chemins stables remplacés à chaque upload, donc `upsert`. Second garde-fou symétrique sur `storage.objects`. ⚠️ **Clause « grandfather » temporaire** (arbitrage produit, voie (b)) : les chemins à 1ᵉʳ segment non-UUID restent écrivables/supprimables **par les membres de CAC**, sans quoi les objets antérieurs deviendraient non supprimables et « supprimer une actu » casserait en prod. Reliquat tracé dans `MULTI_TENANT.md` §2.4, à fermer par une PR de nettoyage (déplacer les objets + réécrire les URL en base **et dans le corps markdown**) dès qu'un 2ᵉ club réel existera. **Rollback** documenté en pied de migration et **symétrique** (le legacy n'a pas été déplacé). Côté applicatif : `src/lib/storage.ts` + `src/lib/clubConfig.ts` + `src/hooks/useClubConfig.ts` (voir tables ci-dessus) et les 4 points d'upload préfixés. **Aucun formulaire de configuration** — c'est PR6b. **Aucun changement PWA** : elle n'écrit dans aucun bucket, et le `runtimeCaching` du SW filtre sur le préfixe `/storage/v1/object/public/`, insensible au segment ajouté.

- **Multi-tenant — gestion des membres (PR5-bis)** : **aucune migration** — `club_members` garde ses deux policies `SELECT` (`club_members_select_own` PR1, `club_members_select_super_admin` PR5) et son seul `GRANT SELECT`. Lecture **et** écritures passent par la nouvelle Edge Function `club-members` en service role (cf. table ci-dessus) : l'email et `last_sign_in_at` vivant dans `auth.users`, le service role est de toute façon obligatoire pour que la liste soit lisible — avoir en plus des policies RLS donnerait deux sources pour la même liste, sur la table qui porte l'autorisation de toute l'app. Côté BO : `MembersPage` (`/admin/members`, `adminOnly`) **absorbe** `InvitePage` (supprimée ; `/admin/invite` redirige en permanence), la carte du dashboard devient « Membres », et le parsing d'erreur non-2xx est extrait dans `lib/functions.ts` (partagé `invite-user` / `club-members`). `invite-user` est **inchangée**. Le garde-fou « au moins un admin par club » (script PR4 §3) est porté **côté serveur** dans la function ; le front ne fait que désactiver les contrôles. ⚠️ Déploiement **manuel** de la function : `supabase functions deploy club-members` (dev puis prod). Rollback : supprimer la function, revenir au script SQL PR4 — aucun SQL à défaire. Dette **non** traitée ici (PR dédiée) : le rôle reste du masquage UI, `tenant_isolation` cloisonne par club et pas par rôle (`MULTI_TENANT.md` §4.1).
- **Matches par équipe — championnat terminé** : migration `supabase/migrations/20260820_team_competitions_terminee.sql`, idempotente. `ALTER TABLE team_competitions ADD COLUMN IF NOT EXISTS terminee BOOLEAN NOT NULL DEFAULT false`. Pas de policy à ajouter (`team_competitions` est déjà couverte par `tenant_isolation`), **pas de GRANT de colonne** non plus : le `GRANT` de `20260606_team_matches.sql` porte sur la table entière sans liste de colonnes — contrairement à `profiles` (cf. `2026081801`), la nouvelle colonne est donc immédiatement écrivable par `authenticated`. Le flag est **explicite** (case à cocher dans `/team-matches/admin`), jamais dérivé d'un calcul sur les étapes : une compétition peut être finie alors qu'une phase finale reste programmée.

---

## Migrations Supabase

Les migrations vivent dans `supabase/migrations/`, nommées `YYYYMMDD_<nom_court>.sql`. Application via le dashboard Supabase (SQL Editor) ou `supabase db push`.

> ⚠️ La CLI dérive la `version` des chiffres de tête du nom de fichier : deux migrations le même jour doivent porter un suffixe de séquence à 2 chiffres (`YYYYMMDDNN`, ex. `2026042601_actus.sql` / `2026042602_actus_image_urls.sql`), sinon `db push` échoue sur `schema_migrations_pkey`.

### Convention GRANTs (obligatoire à partir d'oct. 2026)

À partir du 30 octobre 2026, Supabase n'expose plus automatiquement les tables du schéma `public` à l'API Data : un `GRANT` explicite par rôle est requis. **Chaque `CREATE TABLE` dans `public` doit donc être immédiatement suivi de ses `GRANT`**, alignés sur les policies RLS prévues.

Template :

```sql
CREATE TABLE IF NOT EXISTS ma_table (...);
ALTER TABLE ma_table ENABLE ROW LEVEL SECURITY;

-- Policies RLS
-- CREATE POLICY ... ON ma_table ...

-- GRANTs (à aligner sur les rôles cités dans les policies)
GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE public.ma_table TO authenticated;
GRANT SELECT                         ON TABLE public.ma_table TO anon;
```

Règle pratique : ne grant que ce que la RLS autorise (principe du moindre privilège). Si une table n'est pas exposée à la PWA publique, retirer la ligne `anon`.

---

## PWA — `pwa/`

Projet Vite/React autonome dans le dossier `pwa/`. Consomme les tables Supabase en lecture via le rôle `anon` ; bascule sur le rôle `authenticated` pour la gestion des lives (création de match, démarrage/saisie/suppression). Auth via `supabase.auth.signInWithPassword` (mêmes comptes que le BO).

Stack : React 19, TypeScript, Vite, Tailwind CSS v4, `vite-plugin-pwa`, TanStack Query, React Router v7, Supabase JS.

Déploiement : projet Vercel séparé, Root Directory = `pwa/`.

> **Service Worker** — Le `runtimeCaching` de `VitePWA` (cf. `pwa/vite.config.ts`) ne couvre **que les assets Supabase Storage** (`/storage/v1/object/public/...`, buckets `actu-images` et `event-images`). L'API REST (`/rest/v1/...`), l'auth (`/auth/v1/...`) et Realtime ne sont **jamais** cachés par le SW — sinon les GET `live_matches` (et autres tables) seraient servis périmés après un PATCH, et la mise à jour live ne se verrait qu'au prochain event Realtime. Ne pas réintroduire de pattern large type `url.hostname.includes('supabase.co')`.

### Structure `pwa/src/`

| Fichier | Rôle |
|---|---|
| `App.tsx` | Routes + guard `RequireAuth` (redirige `/login` avec `state.from`) |
| `lib/supabase.ts` | Client Supabase avec `persistSession: true` + `autoRefreshToken: true` (durée du JWT à régler dans le dashboard Supabase) |
| `lib/pwa.ts` | Helper `isStandalone()` — détecte si l'app tourne en mode PWA installée (display-mode standalone ou iOS Safari `navigator.standalone`) |
| `contexts/ClubContext.tsx` | **Copie** de `src/contexts/ClubContext.tsx` (BO) — `ClubProvider` + `useClub()`, résolution du tenant par hostname. Englobe `<HeaderActionProvider>` dans `pwa/src/App.tsx` (gate `loading`). Toutes les queries PWA filtrent par `clubId` (+ `clubId` ajouté aux `queryKey` TanStack Query). Porte le même écran bloquant PR3 que le BO quand la résolution échoue. À synchroniser manuellement avec le BO. |
| `hooks/useAuth.ts` | Hook React partagé : retourne `{ user, loading }`, écoute `onAuthStateChange` |
| `hooks/useInstallPrompt.ts` | Hook qui gère la bannière d'installation : capture `beforeinstallprompt` (Android), détecte iOS Safari, gère le dismiss 7 jours via `localStorage` (`cac:installPromptDismissedAt`). Retourne `{ variant, promptInstall, dismiss }`. |
| `hooks/usePullToRefresh.ts` | Hook générique tirer-pour-rafraîchir basé sur Pointer Events. S'active uniquement sur `pointerType === 'touch'` quand `containerRef.scrollTop === 0`. Retourne `{ pullProgress, isDragging }`. Seuil interne de 80 px ; déclenche `onRefresh` au release si seuil atteint. |
| `liveScoreRules.ts` | **Copie** de `src/liveScoreRules.ts` (BO). À synchroniser manuellement si les règles de score changent. |
| `types.ts` | Types partagés copiés depuis le BO + types `Actu` / `ActuFocalPoint` PWA |
| `utils/focalPoint.ts` | Helper `focalPointStyle(fp)` → renvoie `{ objectPosition: 'x% y%' }` (fallback `50% 50%` si null/undefined). Utilisé par `ActuCard` et `ActuDetailPage`. |
| `pages/LoginPage.tsx` | Formulaire email/mot de passe → redirige sur `state.from ?? /matches` |
| `pages/ActuPage.tsx` | Onglet **Actu** (`/actu`) — fusion Actualités + Événements. Sous-onglets soulignés gérés par `?tab=actus\|events` (`useSearchParams`), monte `<ActusFeed />` ou `<EventsFeed />`. |
| `pages/MatchesEquipesPage.tsx` | Onglet **Match équipes** (`/matches-equipes`, lecture) — rencontres interclubs à venir/passées, filtres saison + équipe + segmented. 3 queries React Query (saisons / compétitions+équipes / rencontres en 2 round-trips `team_etapes`→`team_rencontres`, jointure client). `team_match_lines` jamais requêté. |
| `components/actu/ActuTabSwitcher.tsx` | Onglets soulignés (style iOS Mail) Actualités / Événements. |
| `components/actu/ActusFeed.tsx` | Flux actus (ex-`ActusPage` : `useInfiniteQuery` + load-more + PTR). |
| `components/actu/EventsFeed.tsx` | Flux événements (ex-`EventsPage` : `useQuery` + PTR). |
| `components/matchesEquipes/labels.ts` | Helpers d'affichage `formatGenre`, `formatCategorie`, `competitionShortLabel` (lecture PWA, pas de React). |
| `components/matchesEquipes/MatchEquipeFilterBar.tsx` | Chips Saison + Équipe + segmented À venir / Passés avec compteurs. |
| `components/matchesEquipes/MatchEquipeFilterSheet.tsx` | Bottom sheet générique (liste d'options + check) ouvert au clic sur un chip. |
| `components/matchesEquipes/MatchEquipeCell.tsx` | Cellule rencontre — états `upcoming` (date + contexte + lieu 🏠/✈️) / `past` (colonne résultat vert/rouge/jaune + score). Phase finale → `1/4`,`1/2`,`Finale`. |
| `components/matchesEquipes/MatchEquipeList.tsx` | Liste des cellules + états vides (expose le type `EnrichedRencontre`). |
| `components/icons/TeamMatchesIcon.tsx` | Icône bouclier + coche de l'onglet Match équipes (stroke). |
| `pages/MatchesPage.tsx` | Liste des matchs (Realtime), bouton « Déconnexion », FAB « + » → `/matches/new` (visibles uniquement si auth). Batch-fetch des profils des gestionnaires (`scored_by`) après chargement des matchs, passé en prop `profilesMap` à chaque `MatchCard`. |
| `pages/NewMatchPage.tsx` | Formulaire création de match (iso BO), `status='pending'`, `scored_by=null` |
| `pages/LiveMatchPage.tsx` | Saisie du score (route `/matches/:id/score`). Garde l'accès initial : redirige avec flash si `pending` ou si live appartient à un autre user. Abonnement Realtime filtré sur l'ID du match : si `scored_by` change pendant que l'user est sur la page, bandeau *« lecture seule »* + `forceDisabled` passé au `LiveScoreEntry`. |
| `components/matches/MatchCard.tsx` | Carte avec actions conditionnelles : Démarrer / Reprendre+Libérer / **Prendre le contrôle** (live d'un autre user — ouvre une modale de warning avec le nom du gestionnaire actuel, lu dans `profilesMap`) / Voir+Supprimer (selon auth + ownership). |
| `components/matches/LiveScoreEntry.tsx` | Composant +/- (adapté du BO, layout mobile) |
| `components/install/InstallBanner.tsx` | Bannière fixe au-dessus de la `BottomNav` qui invite à installer la PWA. Variante Android (CTA `beforeinstallprompt`) + variante iOS (instructions Partager → Sur l'écran d'accueil). Pose la classe `has-install-banner` sur `<body>` pour ajuster `padding-bottom` de `.pwa-content`. |
| `components/layout/AppHeader.tsx` | Header fixe route-aware (56px). Mode `root` (tabs) : logo + titre. Mode `sub` : back button (libellé court sur iOS, icône seule sur Android via `navigator.userAgent`) + titre. Affiche optionnellement une action droite tirée de `HeaderActionContext`. |
| `components/layout/BottomNav.tsx` | Navigation fixe basse — 3 onglets (Actu / Match équipes / Live). Le label `Actu` (compact) diverge du titre header `Actualités` (seul cas). |
| `components/layout/PullToRefreshWrapper.tsx` | Wrapper scrollable (`h-full overflow-y-auto`) consommé par `ActusPage` et `EventsPage`. Indicateur SVG inline (couleur `text-primary`) qui se translate et tourne pendant le tirage, `animate-spin` pendant le rechargement, transition `transform 0.2s ease` lors du snap-back. `overscroll-behavior-y: contain` pour neutraliser le PTR natif iOS/Android. Repose sur `usePullToRefresh`. |
| `components/layout/headerConfig.ts` | Map des routes → `HeaderConfig` (`mode`, `title`, `backTo`, `backLabel`). Helper `resolveHeader(pathname)` consommé par `AppHeader`. Ajouter une route ici quand on en crée une. |
| `components/layout/HeaderActionContext.tsx` | Provider + hook `useHeaderAction(action)` pour qu'une page pose une action droite dans le header (text / icon / accent). `onClick` lu via ref → pas besoin de mémoïsation côté appelant. |

### Routes PWA

| Route | Auth | Rôle |
|---|---|---|
| `/login` | publique | Connexion |
| `/actu` (+ `/actus/:id`, `/evenements/:id`) | publiques | Onglet Actu fusionné (lecture `anon`) |
| `/actus`, `/evenements` | publiques | Compat : redirigent vers `/actu?tab=…` |
| `/matches-equipes` | publique | Match équipes (lecture `anon`) |
| `/matches` (+ ses détails) | publiques | Live (lecture `anon`) |
| `/matches/new` | requise | Création d'un match |
| `/matches/:id/score` | requise (et ownership pour `live`) | Saisie / consultation d'un live |

Spec fonctionnelle complète : `docs/specs/PWA.MD` et `docs/specs/PWA_LIVE_AUTH.md`.
